/**
 * gradlew jar コマンド実行時の処理の設定
 */

// gradlew jar コマンド実行時の処理
jar {
    manifest {
        attributes 'Implementation-Title': 'Git Publisher Gradle Plugin', 'Implementation-Version': version
    }
}
